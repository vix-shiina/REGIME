git branch //Voir sur quelle branche tu es
git branch //Voir toutes les branches
git branch nom_branche //Créer une branche
git switch nom_branche //Basculer sur une branche existante
git fetch //Actualiser les infos des branches distantes
git status //Voir les changements


<!-- afficher info vscode bash -->
git config user.name
git config user.email
git config --list

<!-- Modifier les info -->
git config --global user.name "TonNom"
git config --global user.email "tonmail@example.com"